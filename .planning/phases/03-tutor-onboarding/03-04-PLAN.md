---
phase: 03-tutor-onboarding
plan: 04
type: execute
wave: 3
depends_on: [03-02, 03-03]
files_modified: []
autonomous: false
requirements: [TUTR-01, TUTR-02, TUTR-03, TUTR-04]

must_haves:
  truths:
    - "Full tutor flow works end-to-end: sign-up with toggle -> create classroom -> Profile tab shows My Classroom -> edit settings"
    - "Student flow unchanged: sign-up without toggle -> goal-selection -> Profile tab shows placeholder"
  artifacts: []
  key_links: []
---

<objective>
Verify the complete tutor onboarding flow end-to-end on a real device or simulator. Confirm all four requirements (TUTR-01 through TUTR-04) work together.

Purpose: Catches integration issues, navigation guard races, and RLS problems that unit tests cannot cover.
Output: Verified working flow or list of issues to fix.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-tutor-onboarding/03-CONTEXT.md
@.planning/phases/03-tutor-onboarding/03-UI-SPEC.md
@.planning/phases/03-tutor-onboarding/03-02-SUMMARY.md
@.planning/phases/03-tutor-onboarding/03-03-SUMMARY.md
</context>

<threat_model>
## Threat Model — Plan 04: Verification Checkpoint

No new code produced. This plan verifies existing security properties:
- RLS enforcement on tutors and classrooms tables
- Auth guard behavior for tutor vs student paths

| Threat | Severity | Mitigation |
|--------|----------|------------|
| None new | — | Verification-only plan |
</threat_model>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: End-to-end verification of tutor onboarding flow</name>
  <files>src/app/(auth)/sign-up.tsx, src/app/(auth)/create-classroom.tsx, src/app/(tabs)/profile.tsx, src/app/(tabs)/classroom-settings.tsx</files>
  <action>
    Verify the complete tutor onboarding flow manually on device or simulator. Run `npx expo start` and execute the following 5 test scenarios:

    **Test 1: Tutor Sign-Up Flow (TUTR-01, TUTR-02, TUTR-03)**
    1. Navigate to sign-up screen
    2. Confirm "I want to teach" toggle appears below password field, defaulting to OFF
    3. Toggle it ON (thumb should turn coral/accent color)
    4. Enter email + password and tap "Create account"
    5. Confirm you land on "Create your classroom" screen (NOT goal-selection, NOT tabs)
    6. Confirm price field shows "180" pre-filled with "R" prefix
    7. Enter classroom name (e.g. "Physio Year 2")
    8. Type a subject, tap "Add" — confirm tag chip appears
    9. Add a second subject — confirm both chips show
    10. Tap X on one chip — confirm it's removed
    11. Tap "Create classroom" — confirm it navigates to the tabs screen

    **Test 2: Student Sign-Up (unchanged behavior)**
    1. Sign out (or use a different email)
    2. Sign up with toggle OFF
    3. Confirm you land on goal-selection screen (not create-classroom)

    **Test 3: Profile Tab Tutor View (TUTR-04 setup)**
    1. Log in as the tutor created in Test 1
    2. Navigate to Profile tab
    3. Confirm "My Classroom" card shows with the classroom name you entered
    4. Confirm "Manage your classroom" subtitle text appears
    5. Confirm school icon is visible on the card

    **Test 4: Edit Classroom (TUTR-04)**
    1. Tap "My Classroom" card
    2. Confirm classroom-settings screen loads with all fields pre-filled (name, subjects, bio, price)
    3. Change the classroom name
    4. Add or remove a subject
    5. Tap "Save changes"
    6. Confirm you return to Profile tab
    7. Confirm the "My Classroom" card shows the updated classroom name

    **Test 5: Non-Tutor Profile View**
    1. Log in as a student (non-tutor account)
    2. Navigate to Profile tab
    3. Confirm you see the placeholder text "Your profile and settings will appear here" (not the My Classroom card)
  </action>
  <acceptance_criteria>
    - Test 1: Tutor lands on create-classroom after sign-up with toggle ON
    - Test 1: Price field shows "180" by default
    - Test 1: Subject tags can be added and removed
    - Test 1: After classroom creation, tutor lands in tabs
    - Test 2: Student without toggle lands on goal-selection
    - Test 3: Profile tab shows "My Classroom" card for tutor
    - Test 4: Settings screen pre-fills, save works, changes reflected on Profile
    - Test 5: Non-tutor profile shows placeholder only
  </acceptance_criteria>
  <verify>
    <automated>npx tsc --noEmit && npx jest --passWithNoTests</automated>
  </verify>
  <done>All 5 manual test scenarios pass. Tutor flow and student flow both work correctly. No navigation guard race conditions. No RLS errors.</done>
</task>

</tasks>

<verification>
All 4 TUTR requirements verified through manual end-to-end testing.
</verification>

<success_criteria>
- All 5 test scenarios pass
- No navigation guard race conditions observed
- No RLS errors during classroom creation or editing
- Both tutor and student paths work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/03-tutor-onboarding/03-04-SUMMARY.md`
</output>
