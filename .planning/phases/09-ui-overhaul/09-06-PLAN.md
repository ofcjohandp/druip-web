---
phase: 09-ui-overhaul
plan: 06
type: execute
wave: 4
depends_on: [09-03, 09-04, 09-05]
files_modified: []
autonomous: false
requirements: [UI-01, UI-02, UI-03, UI-04, UI-05, UI-06, UI-07, UI-08, UI-09, UI-10, UI-11, UI-12]
must_haves:
  truths:
    - "No hardcoded hex color values exist in any screen or component file (only theme.ts and shadowColor: '#000')"
    - "All screens render without crash on iOS simulator"
    - "TypeScript compiles with zero errors"
  artifacts: []
  key_links: []
---

<objective>
Run final validation: type check, hardcoded color audit, and visual checkpoint on simulator.

Purpose: Confirms the entire UI overhaul is complete — no regressions, no orphaned hardcoded colors, all screens render correctly.

Output: Verified app ready for device testing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/09-ui-overhaul/09-CONTEXT.md
@.planning/phases/09-ui-overhaul/09-03-SUMMARY.md
@.planning/phases/09-ui-overhaul/09-04-SUMMARY.md
@.planning/phases/09-ui-overhaul/09-05-SUMMARY.md
</context>

<threat_model>
Verification only — no code changes. No security surface.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Run type check and hardcoded color audit</name>
  <files></files>
  <read_first>
    - src/features/ui/theme.ts (confirm it's the only file with hex color definitions)
  </read_first>
  <action>
Run these validation commands:

1. TypeScript check:
```bash
npx tsc --noEmit
```
Must pass with zero errors.

2. Hardcoded color audit across all source files:
```bash
grep -rn '#[0-9A-Fa-f]\{3,8\}' src/app/ src/features/ --include='*.tsx' --include='*.ts' | grep -v 'theme.ts' | grep -v '__tests__' | grep -v 'node_modules'
```
Acceptable exceptions:
- `shadowColor: '#000'` — universal shadow convention
- `'#F4F3F4'` — iOS/Android standard Switch off-thumb color
- Any hex inside a comment

Any other hardcoded hex found in screen/component files is a failure — fix it by replacing with the appropriate COLORS token.

3. Verify all new components exist:
```bash
ls src/features/ui/Input.tsx src/features/ui/Tag.tsx src/features/ui/Avatar.tsx
```

4. Verify font package is installed:
```bash
grep '@expo-google-fonts/syne' package.json
```

5. Verify confetti package is installed:
```bash
grep 'react-native-confetti-cannon' package.json
```

If any hardcoded hex values are found (beyond exceptions), fix them by importing the appropriate COLORS token and replacing the value.
  </action>
  <verify>
    <automated>cd "/Users/johanduplessis/Desktop/Claude Code/Druip" && npx tsc --noEmit 2>&1 | tail -3 && echo "---" && grep -rn '#[0-9A-Fa-f]\{3,8\}' src/app/ src/features/ --include='*.tsx' --include='*.ts' | grep -v 'theme.ts' | grep -v '__tests__' | grep -v node_modules | grep -v 'shadowColor' | grep -v 'F4F3F4' | grep -v '//' | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits with code 0
    - Hardcoded hex audit returns 0 violations (excluding shadowColor and Switch thumb)
    - Input.tsx, Tag.tsx, Avatar.tsx all exist in src/features/ui/
    - package.json contains @expo-google-fonts/syne
    - package.json contains react-native-confetti-cannon
  </acceptance_criteria>
  <done>TypeScript clean. Zero hardcoded colors outside theme.ts. All new components and packages present.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification on simulator</name>
  <files></files>
  <action>
Present the UI overhaul for visual verification. Start the Expo dev server with `npx expo start` and instruct the user to review all screens on iOS Simulator.
  </action>
  <verify>User confirms visual approval</verify>
  <done>User types "approved" confirming the visual overhaul looks correct.</done>
  <what-built>Complete UI overhaul: new terracotta/coral brand palette, Syne display font, redesigned auth screens with branded headers, pill progress bar for onboarding, ClassroomCard with avatar + tags + price badge, confetti on subscribe, branded DM bubbles, updated tab bar.</what-built>
  <how-to-verify>
1. Run `npx expo start` and open in iOS Simulator
2. Check sign-in screen: terracotta header with "Druip" branding above form, Input components for email/password, Button with loading state
3. Sign in and check tab bar: white background, coral active icon, warm grey inactive
4. Check home/discovery: styled section headers, ClassroomCard with avatar circle + subject tag pills + terracotta price badge
5. Tap a classroom: classroom-detail with TYPOGRAPHY headings and terracotta price
6. If not subscribed, tap subscribe: subscribe-confirm should show confetti burst on success
7. Open DM chat: terracotta sent bubbles (right), white received bubbles (left), rounded input bar
8. Check profile: Avatar component, TYPOGRAPHY heading
9. Check any onboarding step (sign out + sign up as new user): pill progress bar (not dots), styled selection lists
10. Overall feel: warm, bold, distinctly African — not corporate or pastel
  </how-to-verify>
  <resume-signal>Type "approved" if the visual overhaul looks correct, or describe any issues to fix.</resume-signal>
</task>

</tasks>

<verification>
1. TypeScript: `npx tsc --noEmit` passes
2. Hardcoded hex audit: zero violations
3. Visual: human confirms all screens look correct on simulator
</verification>

<success_criteria>
- All screens render without crash
- Visual identity is warm, bold, branded — not the old minimal white/grey/coral
- No TypeScript errors
- No hardcoded colors outside theme.ts
- User approves the visual overhaul
</success_criteria>

<output>
After completion, create `.planning/phases/09-ui-overhaul/09-06-SUMMARY.md`
</output>
