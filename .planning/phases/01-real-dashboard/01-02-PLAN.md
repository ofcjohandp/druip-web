---
phase: 01-real-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified: []
autonomous: false
requirements:
  - DASH-01
  - DASH-02
  - DASH-03
  - DASH-04

must_haves:
  truths:
    - "Human has verified dashboard renders correctly for a new user (empty states visible)"
    - "Human has verified streak shows 0 (not 12)"
    - "Human has verified no hardcoded fake data is visible"
  artifacts: []
  key_links: []
---

<objective>
Human verification of the live dashboard in the browser to confirm all four DASH requirements work end-to-end.

Purpose: Automated build checks cannot verify visual correctness, empty state rendering, or that the Stitch design is faithfully reproduced. A human must confirm the dashboard looks right.
Output: Human approval or list of issues to fix.
</objective>

<threat_model>
No new threat surface — this is a verification-only plan. No code changes.
</threat_model>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-real-dashboard/01-01-SUMMARY.md
@.planning/phases/01-real-dashboard/01-UI-SPEC.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Verify dashboard with real data and empty states</name>
  <files>druip-web/src/app/(dashboard)/home/page.tsx</files>
  <read_first>
    - .planning/phases/01-real-dashboard/01-UI-SPEC.md (visual spec to verify against)
  </read_first>
  <action>
    Present the human verification checklist. Start the dev server if not running (`cd druip-web && npm run dev`), then guide the user through the verification steps below.

    **Test 1 — New user empty states (DASH-01, DASH-02, DASH-03):**
    1. Sign in as a user with no subscriptions and no tutor rows in DB
    2. Visit http://localhost:3000/home
    3. Verify: Greeting shows real first name (or "Scholar" if no name set)
    4. Verify: Streak card shows "0" (not "12")
    5. Verify: Hero subtext says "Start your streak today!"
    6. Verify: NO spotlight bento card visible (the large "CONTINUE LEARNING" card should be absent)
    7. Verify: Continue Learning section shows empty state: "No classrooms joined yet." heading with "Browse Tutors" button
    8. Verify: Tutors of the Week section shows empty state: "No tutors yet." heading with "Browse Classrooms" button
    9. Verify: Bottom nav — only Home tab has visible text label; Study/Classroom/Profile are icon-only

    **Test 2 — With data (if you have seeded data):**
    1. If tutors exist in DB, verify Tutors of the Week grid shows real tutor names (not "Mandla K." etc.)
    2. If subscriptions exist, verify Continue Learning shows real classroom names
    3. If subscriptions exist, verify spotlight card shows most recent classroom name with "Go to Classroom" button

    **Test 3 — Visual quality:**
    1. Compare dashboard layout to Stitch reference (`stitch_druip_tutor_app_redesign/home_dashboard/code.html`)
    2. Check spacing, colors, and typography match the UI-SPEC
  </action>
  <verify>
    <automated>cd "/Users/johanduplessis/Desktop/Claude Code/Druip/druip-web" && npx next build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - Human confirms empty states render correctly for new user
    - Human confirms streak shows 0 (not hardcoded 12)
    - Human confirms no fake data is visible anywhere on the dashboard
    - Human confirms bottom nav inactive tabs are icon-only
  </acceptance_criteria>
  <done>Human has typed "approved" confirming all DASH requirements are visually and functionally correct.</done>
</task>

</tasks>

<verification>
Human approval of visual and functional correctness.
</verification>

<success_criteria>
- Human types "approved" after verifying all dashboard states
- Phase 1 can be marked complete
</success_criteria>

<output>
After completion, create `.planning/phases/01-real-dashboard/01-02-SUMMARY.md`
</output>
