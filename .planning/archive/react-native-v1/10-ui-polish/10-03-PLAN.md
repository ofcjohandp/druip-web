---
phase: 10-ui-polish
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/features/ui/Tag.tsx
  - src/features/student/ClassroomCard.tsx
autonomous: true
requirements:
  - UIPOL-03

must_haves:
  truths:
    - "Tag pills rendered on a dark card (COLORS.card = #1C2F42) are visually distinct — their background is clearly lighter than the card"
    - "ClassroomCard has a 1px border instead of drop shadow"
    - "The Avatar inside ClassroomCard has a 2px accent-colored ring around it"
    - "No hardcoded hex color values exist in Tag.tsx or ClassroomCard.tsx"
    - "TypeScript compiles clean with zero errors"
  artifacts:
    - path: "src/features/ui/Tag.tsx"
      provides: "pillDefault background changed from COLORS.card to COLORS.surface"
    - path: "src/features/student/ClassroomCard.tsx"
      provides: "card style uses borderWidth:1/borderColor instead of shadow; avatar wrapped in accent ring View"
  key_links:
    - from: "Tag pillDefault"
      to: "COLORS.surface (#152232)"
      via: "backgroundColor: COLORS.surface in styles.pillDefault"
      pattern: "pillDefault.*surface|surface.*pillDefault"
    - from: "ClassroomCard avatar"
      to: "COLORS.accent border ring"
      via: "wrapper View with borderWidth:2, borderColor: COLORS.accent, borderRadius: 24"
      pattern: "avatarRing|borderColor.*accent"
---

<objective>
Fix two visual contrast issues found on device. Tags rendered on ClassroomCards (which use COLORS.card as background) are invisible because Tags also use COLORS.card — same color on same color. Fixing Tag's pillDefault background to COLORS.surface creates the needed contrast. Additionally, ClassroomCard's drop shadow renders poorly against the dark background — replace with a border. Add an accent-colored ring around the Avatar for visual pop consistent with the Duolingo-style brand.

Purpose: ClassroomCard must feel polished and scannable. Tags must be legible.
Output: Tag contrast fixed. ClassroomCard border + avatar ring added.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/features/ui/theme.ts
@src/features/ui/Tag.tsx
@src/features/student/ClassroomCard.tsx

<interfaces>
<!-- Color values for reference -->
COLORS.background = '#0D1B26'   -- darkest
COLORS.surface    = '#152232'   -- slightly lighter than card
COLORS.card       = '#1C2F42'   -- card bg (ClassroomCard uses this)
COLORS.border     = '#2A3F52'   -- subtle blue-grey border
COLORS.accent     = '#FF6B2B'   -- vivid orange

<!-- Current Tag pillDefault (the bug) -->
pillDefault: {
  backgroundColor: COLORS.card,    // same as ClassroomCard background — invisible
  borderColor: COLORS.border,
}

<!-- Target state after fix -->
pillDefault: {
  backgroundColor: COLORS.surface, // #152232 — darker than card, creates contrast
  borderColor: COLORS.border,
}

<!-- Avatar component interface (from src/features/ui/Avatar.tsx) -->
<Avatar name={string} size={number} />
// Renders a circle of given `size` diameter with initials or image
// Wrapping in a View with borderRadius = (size/2 + ringWidth) creates a ring
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix Tag contrast — pillDefault background to COLORS.surface</name>
  <files>src/features/ui/Tag.tsx</files>
  <action>
In src/features/ui/Tag.tsx, change the `pillDefault` StyleSheet entry:

  Before:
    pillDefault: {
      backgroundColor: COLORS.card,
      borderColor: COLORS.border,
    },

  After:
    pillDefault: {
      backgroundColor: COLORS.surface,
      borderColor: COLORS.border,
    },

That is the only change in this file. No other styles, no fontWeight, no new imports.

Verify: no hardcoded hex values anywhere in Tag.tsx — all colors must reference COLORS.*. Confirm `pillSelected` is unchanged (accent color, correct as-is per CONTEXT.md D-03).
  </action>
  <verify>
    <automated>grep -n "backgroundColor\|borderColor\|hex\|#[0-9A-Fa-f]" /Users/johanduplessis/Desktop/Claude\ Code/Druip/src/features/ui/Tag.tsx</automated>
  </verify>
  <done>Tag.tsx pillDefault uses COLORS.surface. No hardcoded hex values. pillSelected unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: ClassroomCard border + avatar ring and TypeScript check</name>
  <files>src/features/student/ClassroomCard.tsx</files>
  <action>
Two changes to ClassroomCard.tsx:

CHANGE A — Replace shadow with border on card style:
  In styles.card, remove shadow properties and replace with a border:

  Before:
    card: {
      backgroundColor: COLORS.card,
      borderRadius: RADII.card,
      padding: SPACING.md,
      marginBottom: SPACING.sm,
      shadowColor: '#000',
      shadowOffset: { width: 0, height: 3 },
      shadowOpacity: 0.10,
      shadowRadius: 8,
      elevation: 4,
    },

  After:
    card: {
      backgroundColor: COLORS.card,
      borderRadius: RADII.card,
      padding: SPACING.md,
      marginBottom: SPACING.sm,
      borderWidth: 1,
      borderColor: COLORS.border,
    },

CHANGE B — Add accent ring around Avatar:
  In the JSX, the Avatar is rendered directly inside the avatarRow View:
    <Avatar name={tutorEmail} size={40} />

  Wrap it in a ring View:
    <View style={styles.avatarRing}>
      <Avatar name={tutorEmail} size={40} />
    </View>

  Add to StyleSheet:
    avatarRing: {
      width: 48,
      height: 48,
      borderRadius: 24,
      borderWidth: 2,
      borderColor: COLORS.accent,
      alignItems: 'center',
      justifyContent: 'center',
    },

  The ring is 4px larger than the Avatar (40 + 4px padding = 44px visible area + 2px border each side = 48px total).
  No change to avatarRow style needed — the ring View replaces the Avatar directly.

After both changes, verify no hardcoded hex values in ClassroomCard.tsx (the '#000' in shadowColor must be gone).

Run TypeScript check:
  cd /Users/johanduplessis/Desktop/Claude\ Code/Druip && node node_modules/.bin/tsc --noEmit
  </action>
  <verify>
    <automated>cd /Users/johanduplessis/Desktop/Claude\ Code/Druip && node node_modules/.bin/tsc --noEmit && echo "TS_CLEAN" && grep -n "shadowColor\|shadowOffset\|shadowOpacity\|#[0-9A-Fa-f]" src/features/student/ClassroomCard.tsx | grep -v "node_modules"</automated>
  </verify>
  <done>TypeScript exits 0. No shadow properties remain in ClassroomCard.tsx. No hardcoded hex values. avatarRing style exists in StyleSheet and wraps the Avatar in JSX.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/johanduplessis/Desktop/Claude\ Code/Druip
# Tag uses COLORS.surface for pillDefault
grep -n "pillDefault" src/features/ui/Tag.tsx
# ClassroomCard has border and avatarRing
grep -n "borderWidth\|avatarRing\|shadowColor" src/features/student/ClassroomCard.tsx
# No hardcoded hex in either file
grep -n "#[0-9A-Fa-f]" src/features/ui/Tag.tsx src/features/student/ClassroomCard.tsx
# TypeScript clean
node node_modules/.bin/tsc --noEmit && echo "PASS"
```
Expected: pillDefault references surface, avatarRing exists, no shadowColor, no hex literals, TS exits 0.
</verification>

<success_criteria>
- Tag pillDefault background is COLORS.surface — visually distinct on COLORS.card backgrounds
- ClassroomCard has borderWidth:1 + borderColor:COLORS.border instead of shadow
- ClassroomCard Avatar is wrapped in a View with 2px COLORS.accent border ring
- Zero hardcoded hex values in Tag.tsx or ClassroomCard.tsx
- TypeScript exits 0
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish/10-03-SUMMARY.md` with:
- Exact changes made to Tag.tsx and ClassroomCard.tsx
- Confirmation of zero hardcoded hex values in both files
- TypeScript result
</output>
