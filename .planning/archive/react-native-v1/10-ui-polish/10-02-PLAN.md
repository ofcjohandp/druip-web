---
phase: 10-ui-polish
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/(tabs)/study.tsx
  - src/app/(tabs)/index.tsx
  - src/app/(tabs)/profile.tsx
autonomous: true
requirements:
  - UIPOL-02

must_haves:
  truths:
    - "Study tab renders a visible 'Study' title header above all list content before any classroom or topic cards"
    - "The Study header is consistent in style with the Home screen greeting header (Syne_800ExtraBold, same padding)"
    - "Home screen top padding puts the greeting clearly below the iOS status bar with breathing room — no clipping"
    - "Profile screen student card has adequate paddingTop so no element clips the status bar"
    - "TypeScript compiles clean with zero errors"
  artifacts:
    - path: "src/app/(tabs)/study.tsx"
      provides: "Fixed header View above WindowedFlatList with 'Study' title + date subtitle"
      exports: ["StudyScreen"]
    - path: "src/app/(tabs)/index.tsx"
      provides: "Verified/adjusted paddingTop in scroll contentContainerStyle"
    - path: "src/app/(tabs)/profile.tsx"
      provides: "Verified/adjusted scroll paddingTop for safe area clearance"
  key_links:
    - from: "src/app/(tabs)/study.tsx"
      to: "SafeAreaView"
      via: "Fixed header rendered as sibling of WindowedFlatList inside SafeAreaView, not inside ListHeaderComponent"
      pattern: "View.*header.*WindowedFlatList|WindowedFlatList.*header"
---

<objective>
Add a proper header zone to the Study tab and verify consistent top padding on Home and Profile. On device, the Study tab currently shows "Browse Classrooms" at the very top of the screen with no breathing room above it. This plan adds a fixed title row ("Study" + current date) above the FlatList — matching the Home screen's greeting/date pattern. Home and Profile are verified and adjusted if their paddingTop is insufficient.

Purpose: Every main tab must feel intentional — not as if content starts at the hardware edge.
Output: Study tab has a header. All three tabs have consistent, device-safe top padding.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/features/ui/theme.ts
@src/app/(tabs)/study.tsx
@src/app/(tabs)/index.tsx
@src/app/(tabs)/profile.tsx

<interfaces>
<!-- Current Home screen header for reference — Study header must match this visual weight -->
Home header (src/app/(tabs)/index.tsx):
  <View style={styles.header}>  {/* marginBottom: SPACING.lg */}
    <Text style={[TYPOGRAPHY.display, { color: COLORS.text }]}>
      {getGreeting(studentProfile?.first_name)}
    </Text>
    <Text style={[TYPOGRAPHY.body, { color: COLORS.textMuted, marginTop: 4 }]}>
      {new Date().toLocaleDateString('en-ZA', { weekday: 'long', day: 'numeric', month: 'long' })}
    </Text>
  </View>

Home scroll contentContainerStyle: { padding: SPACING.lg, paddingBottom: SPACING.xl }
Profile scroll contentContainerStyle: { padding: SPACING.lg, paddingBottom: SPACING.xl }
Study list contentContainerStyle: { padding: SPACING.md, paddingBottom: SPACING.xl }

TYPOGRAPHY.display = { fontFamily: 'Syne_800ExtraBold', fontSize: 32, lineHeight: 38 }
TYPOGRAPHY.body    = { fontFamily: 'Nunito_400Regular', fontSize: 16, lineHeight: 24 }
SPACING.lg = 24
SPACING.md = 16
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Study screen header zone</name>
  <files>src/app/(tabs)/study.tsx</files>
  <action>
The Study screen uses a SafeAreaView wrapping a WindowedFlatList. Content starts directly inside ListHeaderComponent with "My Classroom" / "Browse Classrooms" section titles — no page-level header above them.

Add a fixed header View as a sibling above the WindowedFlatList inside the SafeAreaView. Do NOT put it inside ListHeaderComponent (that scrolls away — a page title should stay or at minimum be prominent above the first section).

Structure change:
  Before:
    <SafeAreaView style={styles.container}>
      <WindowedFlatList
        ListHeaderComponent={renderHeader}
        ...
      />
    </SafeAreaView>

  After:
    <SafeAreaView style={styles.container}>
      <View style={styles.screenHeader}>
        <Text style={[TYPOGRAPHY.display, { color: COLORS.text }]}>Study</Text>
        <Text style={[TYPOGRAPHY.body, { color: COLORS.textMuted, marginTop: 4 }]}>
          {new Date().toLocaleDateString('en-ZA', { weekday: 'long', day: 'numeric', month: 'long' })}
        </Text>
      </View>
      <WindowedFlatList
        data={topics}
        keyExtractor={(item) => item.id}
        renderItem={renderItem}
        ListHeaderComponent={renderHeader}
        contentContainerStyle={styles.list}
      />
    </SafeAreaView>

Add to StyleSheet:
  screenHeader: {
    paddingHorizontal: SPACING.lg,
    paddingTop: SPACING.md,
    paddingBottom: SPACING.sm,
  }

Note: The loading state SafeAreaView (returned early) does NOT need the header — the spinner is fine there.

TYPOGRAPHY.display is Syne_800ExtraBold per D-01 (typography fix in 10-01 plan confirms no fontWeight needed).
The date format must match Home screen: toLocaleDateString('en-ZA', { weekday: 'long', day: 'numeric', month: 'long' }).
  </action>
  <verify>
    <automated>grep -n "screenHeader\|Study.*header\|View.*style.*screenHeader" /Users/johanduplessis/Desktop/Claude\ Code/Druip/src/app/\(tabs\)/study.tsx</automated>
  </verify>
  <done>study.tsx renders a View with styles.screenHeader containing a "Study" Text in TYPOGRAPHY.display before the WindowedFlatList. The screenHeader style exists in StyleSheet.create.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Home and Profile top padding and run TypeScript check</name>
  <files>src/app/(tabs)/index.tsx, src/app/(tabs)/profile.tsx</files>
  <action>
HOME SCREEN — src/app/(tabs)/index.tsx:
  Current contentContainerStyle: { padding: SPACING.lg, paddingBottom: SPACING.xl }
  SPACING.lg = 24. SafeAreaView already handles the status bar inset on iOS.
  24px padding is adequate. No change needed IF SafeAreaView is working correctly (it is — from CONTEXT.md device evidence, Home looked right).
  Confirm: SafeAreaView is from 'react-native' (not safe-area-context) — already confirmed in the file. No change required.
  If padding feels tight during device review, increase paddingTop to SPACING.xl (32) in contentContainerStyle only — but do not change without device evidence.
  Leave as-is unless visually verifying on device reveals a problem. Document in SUMMARY that Home was verified correct.

PROFILE SCREEN — src/app/(tabs)/profile.tsx:
  Current contentContainerStyle: { padding: SPACING.lg, paddingBottom: SPACING.xl }
  Same as Home — SPACING.lg (24) top padding inside SafeAreaView is adequate.
  CONTEXT.md device evidence confirms "Profile screen looked good — student card concept is correct."
  No change needed. Document as verified.

After reviewing both files, run TypeScript check across all three tab screens:
  cd /Users/johanduplessis/Desktop/Claude\ Code/Druip && node node_modules/.bin/tsc --noEmit

Fix any TypeScript errors (only study.tsx was modified, so errors would come from there).
  </action>
  <verify>
    <automated>cd /Users/johanduplessis/Desktop/Claude\ Code/Druip && node node_modules/.bin/tsc --noEmit && echo "TS_CLEAN"</automated>
  </verify>
  <done>TypeScript exits 0. Home and Profile padding documented as verified (no change required). study.tsx compiles without errors.</done>
</task>

</tasks>

<verification>
```bash
cd /Users/johanduplessis/Desktop/Claude\ Code/Druip
# Confirm Study header exists
grep -n "screenHeader\|Study" src/app/\(tabs\)/study.tsx | head -10
# TypeScript clean
node node_modules/.bin/tsc --noEmit && echo "PASS"
```
</verification>

<success_criteria>
- Study screen source contains a `screenHeader` style and a "Study" title Text rendered before WindowedFlatList
- Date subtitle uses same en-ZA locale format as Home screen
- TypeScript exits 0
- Home and Profile padding verified as adequate (documented, no change)
</success_criteria>

<output>
After completion, create `.planning/phases/10-ui-polish/10-02-SUMMARY.md` with:
- What was added to study.tsx (describe the header block)
- Confirmation that Home and Profile were verified with no changes needed
- Any spacing decisions made
</output>
